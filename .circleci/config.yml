version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:16.20
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: npm install
      - run:
          name: Run Build / Test
          command: npm run build --if-present

workflows:
  main-workflow:
    jobs:
      - build