name: CI Karsajobs Backend

on:
  push:
    branches: [ "main", "karsajobs" ]
  pull_request:
    branches: [ "main", "karsajobs" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Go / Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Install Dependencies
        run: npm install

      - name: Run Tests
        run: npm test --if-present